@model Habitia.ViewModels.LoginViewModel
@{
    ViewData["Title"] = "Iniciar Sesión";
    Layout = "_AuthLayout";
    ViewData["AuthImage"] = "login.png";
}
<h1 class="auth-title">Iniciar sesión</h1>
<p class="auth-subtitle">Completa los campos para iniciar sesión a tu cuenta.</p>
@if (TempData["MensajeExito"] != null)
{
    <div style="background-color: #EAF7EA; color: #336332; border: 1px solid #CDEACD; padding: 12px 16px; border-radius: 10px; margin-bottom: 18px; font-size: 0.88rem; font-weight: 500;">
        <i class="bi bi-check-circle" style="margin-right: 8px;"></i> @TempData["MensajeExito"]
    </div>
}
<form asp-action="Login" method="post" class="auth-form" id="loginForm" novalidate>
    <div asp-validation-summary="All" class="auth-validation-summary"></div>
    <div class="auth-field">
        <label asp-for="Email">Correo</label>
        <div class="auth-input-wrap">
            <input asp-for="Email" class="auth-input" placeholder="correo@ejemplo.com" autocomplete="username" />
            <button type="button" class="auth-input-icon auth-clear" data-target="Email" aria-label="Limpiar correo" tabindex="-1">
                <i class="bi bi-x-circle"></i>
            </button>
        </div>
        <span asp-validation-for="Email" class="auth-error" id="errEmail"></span>
    </div>
    <div class="auth-field">
        <label asp-for="Password">Contraseña</label>
        <div class="auth-input-wrap">
            <input asp-for="Password" class="auth-input" placeholder="••••••••" autocomplete="current-password" />
            <button type="button" class="auth-input-icon auth-toggle-password" data-target="Password" aria-label="Mostrar contraseña" tabindex="-1">
                <i class="bi bi-eye"></i>
            </button>
        </div>
        <span asp-validation-for="Password" class="auth-error" id="errPassword"></span>
    </div>
    <div class="auth-row">
        <label class="auth-checkbox">
            <input asp-for="RememberMe" />
            <span>Recordarme</span>
        </label>
    </div>
    <button type="submit" class="auth-submit" id="btnLogin">Iniciar sesión</button>
    <p class="auth-footer-text">
        ¿No tienes cuenta? <a asp-controller="Account" asp-action="Register" class="auth-link">Crear cuenta</a>
    </p>
</form>
@section Scripts {
    <script src="~/js/login.js"></script>
}

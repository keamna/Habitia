@model Habitia.ViewModels.Acceso.ValidarCodigoVM

@{
    ViewData["Title"] = "Validar Código de Autorización";
}

<div class="container py-4" style="max-width: 500px;">

    <h1 class="h3 mb-4">Validar Código de Autorización</h1>

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger" role="alert">@TempData["Error"]</div>
    }

    <div class="card shadow-sm">
        <div class="card-body">

            <form asp-action="ValidarCodigo" method="post">
                @Html.AntiForgeryToken()

                <div class="mb-3">
                    <label class="form-label">Código de autorización</label>
                    <input asp-for="Codigo" class="form-control form-control-lg text-center" placeholder="Ingrese el código que le dio el residente" autofocus />
                    <span asp-validation-for="Codigo" class="text-danger small"></span>
                    <div class="form-text">
                        El residente le comparte este código al momento de autorizar la visita.
                    </div>
                </div>

                <div class="d-flex gap-2">
                    <a asp-action="Index" class="btn btn-outline-secondary w-50">Cancelar</a>
                    <button type="submit" class="btn btn-primary w-50">Validar</button>
                </div>

            </form>

        </div>
    </div>

</div>

@model Habitia.ViewModels.Acceso.ValidarQRVM

@{
    ViewData["Title"] = "Validar Autorización QR";
}

<div class="container py-4" style="max-width: 500px;">

    <h1 class="h3 mb-4">Validar Autorización</h1>

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger" role="alert">@TempData["Error"]</div>
    }

    <div class="card shadow-sm">
        <div class="card-body">

            <form asp-action="ValidarQR" method="post">
                @Html.AntiForgeryToken()

                <div class="mb-3">
                    <label class="form-label">Código de autorización</label>
                    <input asp-for="Codigo" class="form-control form-control-lg text-center" placeholder="Escanee o escriba el código" autofocus />
                    <span asp-validation-for="Codigo" class="text-danger small"></span>
                    <div class="form-text">
                        Puede escanear el QR con un lector externo (se escribe solo aquí) o ingresar el código manualmente.
                    </div>
                </div>

                <button type="submit" class="btn btn-primary w-100">Validar</button>

            </form>

        </div>
    </div>

</div>